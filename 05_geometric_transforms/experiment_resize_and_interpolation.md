# 实验：缩放与插值

## 实验目标

理解改变图像尺寸时，像素值为什么必须被重新估计。

## 对应代码

- Python：`python/resize_image.py`

## 第一性原理

缩放不是简单把矩阵拉伸，而是要重新决定新位置的像素值。

这就是插值的意义。

## 实验步骤

1. 将同一张图放大和缩小
2. 比较不同插值策略的效果
3. 观察细节与平滑程度的变化

## 预期效果

- 理解放大和缩小时适合的插值策略通常不同
- 能解释为什么缩放结果会影响后续处理效果
